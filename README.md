# HoneyPot

The project works as a fake login website (honeypot) that attracts attackers. It records every login attempt, analyzes the activity for suspicious behavior like brute force, suspicious usernames, and weak passwords, and generates alerts when threats are detected. Legitimate credentials are then redirected to the real website.
